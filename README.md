Java JDK 17 or above version
STS (STS 4 recommended)
 Node JS
 Vs Code
MySQL Server and MySQL WorkBench

For  frontend :npm start command.
--After  npm start , if we get [Error: cannot find module react], then:

npm install --save react react-dom @types/react @types/react-dom    then:

 [npm start] 


 npm install react-scripts --save     --> For Scripts error if any
